import { Route } from '@angular/router';
import { InterceptorsComponent } from './interceptors/interceptors.component';

export const interceptorsRoutes: Route[] = [
	{ path: '', component: InterceptorsComponent },
];
